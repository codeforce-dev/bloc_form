name: bloc_form
description: BlocForm separates form code from layout code.
version: 0.0.1
repository: https://github.com/codeforce-dev/bloc_form

environment:
  sdk: '>=2.19.4 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0